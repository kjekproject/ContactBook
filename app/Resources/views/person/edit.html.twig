{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">EDIT CONTACT</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Save" />
    {{ form_end(edit_form) }}<br/>

    <a class="btn btn-default" href="{{ path('index') }}">Back to the list</a><br/><br/>
    
    <h3 class="text-center">ADD CONTACT DETAILS</h3>
    
    {{ form_start(address_form) }}
        {{ form_widget(address_form) }}
        <input type="submit" value="Add address"/>
    {{ form_end(address_form) }}<br/>
    
    {{ form_start(email_form) }}
        {{ form_widget(email_form) }}
        <input type="submit" value="Add email"/>
    {{ form_end(email_form) }}<br/>
    
    {{ form_start(phone_form) }}
        {{ form_widget(phone_form) }}
        <input type="submit" value="Add phone"/>
    {{ form_end(phone_form) }}<br/>

{% endblock %}
