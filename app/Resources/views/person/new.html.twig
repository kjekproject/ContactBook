{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">ADD NEW CONTACT</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}<br/>
    
    <a class="btn btn-default" href="{{ path('person_index') }}">Back to the list</a>

{% endblock %}
