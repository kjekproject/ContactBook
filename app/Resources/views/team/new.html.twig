{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">CREATE NEW TEAM</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}<br/>

    <a class="btn btn-default" href="{{ path('team_index') }}">Back to the list</a>

{% endblock %}
